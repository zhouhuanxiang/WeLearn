<!DOCTYPE html>
<html>
<head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />

    <!-- 最新 Bootstrap 核心 CSS 文件 -->
    <link rel="stylesheet" href="http://cdn.bootcss.com/twitter-bootstrap/3.0.3/css/bootstrap.min.css">

    <!-- 可选的Bootstrap主题文件（一般不用引入） -->
    <link rel="stylesheet" href="http://cdn.bootcss.com/twitter-bootstrap/3.0.3/css/bootstrap-theme.min.css">

    <!-- jQuery文件。务必在bootstrap.min.js 之前引入 -->
    <script src="http://cdn.bootcss.com/jquery/1.10.2/jquery.min.js"></script>

    <!-- 最新的 Bootstrap 核心 JavaScript 文件 -->
    <script src="http://cdn.bootcss.com/twitter-bootstrap/3.0.3/js/bootstrap.min.js"></script>
</head>
<body>

<div class="center-block" style="background-color:#ccc;">
    <table class="table">
        <caption><h1><%= title %></h1></caption>
        <thead>
        <tr>
            <th>上课时间</th>
            <th>课程名</th>
            <th>上课老师</th>
            <th>上课地点</th>
        </tr>
        </thead>
        <tbody>
        <% for(var j = 1; j < 7; j++) {%>
        <% var find = false %>
        <% for(var i = 0; i < classes.length; i++) {%>
        <% if (classes[i].time[1] === j && classes[i].time[0] === day && classes[i].week[week - 1] === 1) {%>
        <tr class="active">
            <td>第<%= j %>大节</td>
            <td><%= classes[i].coursename %></td>
            <td><%= classes[i].teacher %></td>
            <td><%= classes[i].classroom %></td>
        </tr>
        <% find = true %>
        <% } %>
        <% }%>
        <% if(find === false) {%>
        <tr class="success">
            <td>第<%= j %>大节</td>
            <td></td>
            <td></td>
            <td></td>
        </tr>
        <%}%>
        <% }%>

        </tbody>
    </table>
</div>

</body>
</html>